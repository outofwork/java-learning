plugins {
    id 'java'
}

group 'com.outofwork'
version '1.0'

repositories {
    mavenCentral()
}

test {
    ignoreFailures = true
    //gradle build -x test to skip test
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}